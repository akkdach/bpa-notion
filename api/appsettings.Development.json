{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Information",
      "Microsoft.EntityFrameworkCore.Database.Command": "Information"
    }
  },

  "//": "ค่าสำหรับ `dotnet run` / `dotnet ef` บนเครื่อง",
  "//tracked": "ไฟล์นี้ขึ้น git — จึงมีได้เฉพาะค่าที่ไม่ใช่ความลับ",
  "//missing": "password กับ Jwt:Key อยู่ใน User Secrets — ตั้งด้วย scripts/setup-secrets.ps1",

  "ConnectionStrings": {
    "//target": "PostgreSQL 18.1 (Windows) ที่ 20.198.244.238 — ไม่ใช่ postgres ใน docker-compose",
    "//docker": "ถ้าจะกลับไปใช้ Docker: Host=localhost;Port=5440;Database=projectmanagement;Username=postgres",
    "//password": "ไม่มีในเส้นนี้โดยเจตนา — Postgres:Password จาก User Secrets ถูกเติมให้ตอน startup",
    "//ssl": "⚠️ เซิร์ฟเวอร์นี้ยังไม่เปิด TLS (pg_stat_ssl.ssl = false) รหัสผ่านวิ่งเป็น plaintext",
    "DefaultConnection": "Host=20.198.244.238;Port=5432;Database=project_management;Username=postgres;Include Error Detail=true"
  },

  "Cors": {
    "AllowedOrigins": [
      "http://localhost",
      "http://localhost:5173"
    ]
  },
  "Uploads": {
    "RootPath": "./uploads"
  }
}
